"use client";

// Canvas de proyecto ahora se renderiza directamente desde el layout del proyecto.
// Este componente ya no es necesario pero se mantiene por si se usa en el futuro.
export default function CanvasOverlay({ clientId: _clientId }: { clientId: string }) {
  return null;
}
