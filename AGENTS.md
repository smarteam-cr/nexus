# Nexus — guía para agentes

**Ver @CLAUDE.md** — es la ÚNICA fuente de reglas para agentes (Claude Code, Codex o el que
sea). Este archivo era un fork que derivó y llegó a mentir sobre el gate de build; el
2026-08-01 pasó a ser este puntero y `lib/docs/doc-sync.test.ts` lo congela así.
